# Azure Pipeline for KhulaFxAdminNew (Frontend Angular)
trigger:
  branches:
    include:
      - main
      - master

pool:
  name: 'Default'

variables:
  - group: 'Deployment-Secrets'

stages:
  - stage: NotifyStart
    displayName: 'Notify Build Started'
    jobs:
      - job: Telegram
        displayName: 'Send Telegram Notification'
        steps:
          - task: PowerShell@2
            displayName: 'Send Start Notification'
            inputs:
              targetType: 'inline'
              script: |
                $message = "🚀 *KhulaFxAdmin Frontend Build Started*`n`n"
                $message += "📦 *Project:* KhulaFxAdminNew (Angular)`n"
                $message += "🔢 *Build:* $(Build.BuildNumber)`n"
                $message += "👤 *By:* $(Build.RequestedFor)`n"
                $message += "🌿 *Branch:* $(Build.SourceBranchName)`n"
                $message += "💬 *Commit:* $(Build.SourceVersionMessage)`n"
                $message += "🔗 [View](https://dev.azure.com/KhulaFx/KhulaFxTrade/_build/results?buildId=$(Build.BuildId))"
                
                $body = @{ chat_id = "$(TELEGRAM_CHAT_ID)"; text = $message; parse_mode = "Markdown" } | ConvertTo-Json
                
                try {
                  Invoke-RestMethod -Uri "https://api.telegram.org/bot$(TELEGRAM_BOT_TOKEN)/sendMessage" -Method Post -ContentType "application/json" -Body $body
                } catch { Write-Warning "Telegram failed: $_" }

  - stage: Build
    displayName: 'Build Angular Application'
    dependsOn: NotifyStart
    jobs:
      - job: BuildAngular
        displayName: 'Build Angular'
        steps:
          - checkout: self
            fetchDepth: 1

          - task: NodeTool@0
            displayName: 'Install Node.js'
            inputs:
              versionSpec: $(nodeVersion)

          - task: PowerShell@2
            displayName: 'Set Node Memory Limit'
            inputs:
              targetType: 'inline'
              script: |
                $env:NODE_OPTIONS="--max-old-space-size=4096"
                Write-Host "Set Node memory limit to 4GB"

          - task: Npm@1
            displayName: 'npm install'
            inputs:
              command: 'install'
              workingDir: '.'

          - task: Npm@1
            displayName: 'npm build (production)'
            inputs:
              command: 'custom'
              workingDir: '.'
              customCommand: 'run build -- --configuration production'

          - task: PowerShell@2
            displayName: 'Create Configuration Files'
            inputs:
              targetType: 'inline'
              script: |
                $distFolder = Get-ChildItem -Path "$(System.DefaultWorkingDirectory)" -Filter "browser" -Recurse -Directory | Where-Object { $_.FullName -like "*dist*" } | Select-Object -First 1
                
                if (-not $distFolder) {
                  $distFolder = Get-ChildItem -Path "$(System.DefaultWorkingDirectory)/dist" -Directory | Select-Object -First 1
                }
                
                if ($distFolder) {
                  $distPath = $distFolder.FullName
                  Write-Host "Found dist folder: $distPath"
                } else {
                  Write-Error "Could not find dist folder"
                  exit 1
                }
                
                $webConfig = '<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name="Angular Routes" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
          </conditions>
          <action type="Rewrite" url="/" />
        </rule>
      </rules>
    </rewrite>
    <httpProtocol>
      <customHeaders>
        <add name="X-Frame-Options" value="SAMEORIGIN" />
        <add name="X-Content-Type-Options" value="nosniff" />
        <add name="X-XSS-Protection" value="1; mode=block" />
        <add name="X-Robots-Tag" value="noindex, nofollow" />
      </customHeaders>
    </httpProtocol>
    <staticContent>
      <mimeMap fileExtension=".json" mimeType="application/json" />
      <mimeMap fileExtension=".woff2" mimeType="application/font-woff2" />
    </staticContent>
  </system.webServer>
</configuration>'
                
                $robotsTxt = 'User-agent: *
Disallow: /

User-agent: Googlebot
Disallow: /

User-agent: Bingbot
Disallow: /

User-agent: DuckDuckBot
Disallow: /'
                
                $webConfig | Out-File -FilePath "$distPath/web.config" -Encoding utf8 -Force
                $robotsTxt | Out-File -FilePath "$distPath/robots.txt" -Encoding utf8 -Force
                
                Write-Host "web.config created (FIXED - removed duplicate .woff MIME type)"
                Write-Host "robots.txt created"

          - task: PublishBuildArtifacts@1
            displayName: 'Publish Artifacts'
            inputs:
              pathToPublish: '$(System.DefaultWorkingDirectory)/dist'
              artifactName: 'frontend'

  - stage: Deploy
    displayName: 'Deploy to VPS'
    dependsOn: Build
    condition: succeeded()
    jobs:
      - job: DeployFrontend
        displayName: 'Deploy to IIS'
        steps:
          - download: current
            artifact: frontend

          - task: PowerShell@2
            displayName: 'Find Browser Folder'
            inputs:
              targetType: 'inline'
              script: |
                $browserFolder = Get-ChildItem -Path "$(Pipeline.Workspace)/frontend" -Filter "browser" -Recurse -Directory | Select-Object -First 1
                
                if ($browserFolder) {
                  $deployPath = $browserFolder.FullName
                  Write-Host "Found browser folder: $deployPath"
                  Write-Host "##vso[task.setvariable variable=DeployPath]$deployPath"
                } else {
                  $firstDir = Get-ChildItem -Path "$(Pipeline.Workspace)/frontend" -Directory | Select-Object -First 1
                  if ($firstDir) {
                    $deployPath = $firstDir.FullName
                    Write-Host "Using folder: $deployPath"
                    Write-Host "##vso[task.setvariable variable=DeployPath]$deployPath"
                  } else {
                    Write-Error "Could not find deployment folder"
                    exit 1
                  }
                }

          - task: PowerShell@2
            displayName: 'Clean Existing web.config'
            inputs:
              targetType: 'inline'
              script: |
                $destinationPath = "\\$(vpsServer)\c$\TRADERVPS\Sites\khulafx.com\secure-admin"
                $oldWebConfig = "$destinationPath\web.config"
                
                if (Test-Path $oldWebConfig) {
                  Remove-Item -Path $oldWebConfig -Force
                  Write-Host "Removed old web.config to prevent configuration errors"
                }

          - task: PowerShell@2
            displayName: 'Deploy via Network Share'
            inputs:
              targetType: 'inline'
              script: |
                Write-Host "Deploying Angular Frontend via Network Share"
                
                $sourcePath = "$(DeployPath)"
                $destinationPath = "\\$(vpsServer)\c$\TRADERVPS\Sites\khulafx.com\secure-admin"
                
                Write-Host "Source: $sourcePath"
                Write-Host "Destination: $destinationPath"
                
                try {
                  New-Item -ItemType Directory -Path $destinationPath -Force
                  Write-Host "Directory created/verified"
                  
                  Remove-Item -Path "$destinationPath\*" -Recurse -Force -ErrorAction SilentlyContinue
                  Write-Host "Destination cleaned"
                  
                  Copy-Item -Path "$sourcePath\*" -Destination $destinationPath -Recurse -Force
                  
                  Write-Host "Frontend deployed successfully!"
                  Write-Host "Location: $destinationPath"
                  
                  $sourceFiles = Get-ChildItem -Path $sourcePath -Recurse -File
                  $destFiles = Get-ChildItem -Path $destinationPath -Recurse -File
                  Write-Host "Source files: $($sourceFiles.Count)"
                  Write-Host "Destination files: $($destFiles.Count)"
                  
                  if ($destFiles.Count -gt 0) {
                    Write-Host "Deployment verified - files are in correct location"
                  } else {
                    Write-Error "No files found in destination - deployment failed"
                    exit 1
                  }
                }
                catch {
                  Write-Error "Deployment failed: $_"
                  exit 1
                }

          - task: PowerShell@2
            displayName: 'Verify Files Deployed'
            inputs:
              targetType: 'inline'
              script: |
                Write-Host "Checking deployed files..."
                
                $destinationPath = "\\$(vpsServer)\c$\TRADERVPS\Sites\khulafx.com\secure-admin"
                
                Write-Host "Checking: $destinationPath"
                $files = Get-ChildItem -Path $destinationPath -Recurse -File
                
                if ($files.Count -gt 0) {
                  Write-Host "SUCCESS: $($files.Count) files deployed"
                } else {
                  Write-Error "FAILED: No files found in destination"
                  exit 1
                }

          - task: PowerShell@2
            displayName: 'Test Website'
            inputs:
              targetType: 'inline'
              script: |
                Write-Host "Testing website..."
                Start-Sleep -Seconds 10
                
                $urlsToTest = @("https://khulafx.com/secure-admin", "http://khulafx.com/secure-admin")
                
                $success = $false
                foreach ($url in $urlsToTest) {
                  try {
                    Write-Host "Testing: $url"
                    $response = Invoke-WebRequest -Uri $url -UseBasicParsing -TimeoutSec 30
                    Write-Host "Website Status: $($response.StatusCode)"
                    $success = $true
                    break
                  }
                  catch {
                    Write-Host "$url returned: $($_.Exception.Message)"
                  }
                }
                
                if (-not $success) {
                  Write-Warning "Website might still be configuring..."
                  Write-Host "BUT: Files are deployed to correct IIS location: TRADERVPS\Sites\khulafx.com\secure-admin"
                }

  - stage: NotifyComplete
    displayName: 'Notify Completion'
    dependsOn: Deploy
    condition: always()
    jobs:
      - job: SendResult
        displayName: 'Send Telegram Result'
        steps:
          - task: PowerShell@2
            displayName: 'Send Notification'
            inputs:
              targetType: 'inline'
              script: |
                $success = "$(Agent.JobStatus)" -eq "Succeeded"
                $icon = if ($success) { "✅" } else { "❌" }
                $status = if ($success) { "SUCCESS" } else { "FAILED" }
                
                $message = "$icon *KhulaFxAdmin Frontend Deploy $status!*`n`n"
                $message += "📦 *Project:* KhulaFxAdminNew (Angular)`n"
                $message += "🔢 *Build:* $(Build.BuildNumber)`n"
                $message += "👤 *By:* $(Build.RequestedFor)`n"
                $message += "🌿 *Branch:* $(Build.SourceBranchName)`n"
                $message += "💬 *Commit:* $(Build.SourceVersionMessage)`n"
                
                if ($success) {
                  $message += "🌐 *URL:* https://khulafx.com/secure-admin`n"
                }
                
                $duration = [math]::Round((New-TimeSpan -Start '$(System.PipelineStartTime)' -End (Get-Date)).TotalMinutes, 2)
                $message += "⏱️ *Duration:* $duration minutes`n"
                $message += "🔗 [View Logs](https://dev.azure.com/KhulaFx/KhulaFxTrade/_build/results?buildId=$(Build.BuildId))"
                
                $body = @{ chat_id = "$(TELEGRAM_CHAT_ID)"; text = $message; parse_mode = "Markdown" } | ConvertTo-Json
                
                try {
                  Invoke-RestMethod -Uri "https://api.telegram.org/bot$(TELEGRAM_BOT_TOKEN)/sendMessage" -Method Post -ContentType "application/json" -Body $body
                } catch { Write-Warning "Telegram failed: $_" }
