# Azure Pipeline for KhulaFxAdminNew (Frontend Angular)
# Uses variables from 'Deployment-Secrets' group
# Base Href: /secure-admin/
# Deployment Method: Remote PowerShell (no network share locks)

trigger:
  branches:
    include:
      - main
      - master

pool:
  name: 'Default'

variables:
  - group: 'Deployment-Secrets'

stages:
  - stage: NotifyStart
    displayName: 'Notify Build Started'
    jobs:
      - job: Telegram
        displayName: 'Send Telegram Notification'
        steps:
          - task: PowerShell@2
            displayName: 'Send Start Notification'
            inputs:
              targetType: 'inline'
              script: |
                $message = "🚀 *KhulaFxAdmin Frontend Build Started*`n`n"
                $message += "📦 *Project:* KhulaFxAdminNew (Angular)`n"
                $message += "🔢 *Build:* $(Build.BuildNumber)`n"
                $message += "👤 *By:* $(Build.RequestedFor)`n"
                $message += "🌿 *Branch:* $(Build.SourceBranchName)`n"
                $message += "💬 *Commit:* $(Build.SourceVersionMessage)`n"
                $message += "🔗 [View](https://dev.azure.com/KhulaFx/KhulaFxTrade/_build/results?buildId=$(Build.BuildId))"
                
                $body = @{ chat_id = "$(TELEGRAM_CHAT_ID)"; text = $message; parse_mode = "Markdown" } | ConvertTo-Json
                
                try {
                  Invoke-RestMethod -Uri "https://api.telegram.org/bot$(TELEGRAM_BOT_TOKEN)/sendMessage" -Method Post -ContentType "application/json" -Body $body
                } catch { Write-Warning "Telegram failed: $_" }

  - stage: Build
    displayName: 'Build Angular Application'
    dependsOn: NotifyStart
    jobs:
      - job: BuildAngular
        displayName: 'Build Angular'
        steps:
          - checkout: self
            fetchDepth: 1

          - task: NodeTool@0
            displayName: 'Install Node.js'
            inputs:
              versionSpec: '20.x'

          - task: PowerShell@2
            displayName: 'Set Node Memory Limit'
            inputs:
              targetType: 'inline'
              script: |
                $env:NODE_OPTIONS="--max-old-space-size=4096"
                Write-Host "Set Node memory limit to 4GB"

          - task: Npm@1
            displayName: 'npm install'
            inputs:
              command: 'install'
              workingDir: '.'

          - task: Npm@1
            displayName: 'npm build (production with base-href)'
            inputs:
              command: 'custom'
              workingDir: '.'
              customCommand: 'run build -- --configuration production --base-href=/secure-admin/'

          - task: PowerShell@2
            displayName: 'Create Configuration Files'
            inputs:
              targetType: 'inline'
              script: |
                $distFolder = Get-ChildItem -Path "$(System.DefaultWorkingDirectory)" -Filter "browser" -Recurse -Directory | Where-Object { $_.FullName -like "*dist*" } | Select-Object -First 1
                
                if (-not $distFolder) {
                  $distFolder = Get-ChildItem -Path "$(System.DefaultWorkingDirectory)/dist" -Directory | Select-Object -First 1
                }
                
                if ($distFolder) {
                  $distPath = $distFolder.FullName
                  Write-Host "Found dist folder: $distPath"
                } else {
                  Write-Error "Could not find dist folder"
                  exit 1
                }
                
                # Clean web.config - NO staticContent section to avoid conflicts
                $webConfig = @"
                <?xml version="1.0" encoding="utf-8"?>
                <configuration>
                  <system.webServer>
                    <rewrite>
                      <rules>
                        <rule name="Angular Routes" stopProcessing="true">
                          <match url=".*" />
                          <conditions logicalGrouping="MatchAll">
                            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
                            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
                          </conditions>
                          <action type="Rewrite" url="index.html" />
                        </rule>
                      </rules>
                    </rewrite>
                  </system.webServer>
                </configuration>
                "@
                
                $robotsTxt = @"
                User-agent: *
                Disallow: /
                
                User-agent: Googlebot
                Disallow: /
                
                User-agent: Bingbot
                Disallow: /
                "@
                
                $webConfig | Out-File -FilePath "$distPath/web.config" -Encoding utf8 -Force
                $robotsTxt | Out-File -FilePath "$distPath/robots.txt" -Encoding utf8 -Force
                
                Write-Host "✅ Clean web.config created" -ForegroundColor Green
                Write-Host "✅ robots.txt created" -ForegroundColor Green

          - task: PublishBuildArtifacts@1
            displayName: 'Publish Artifacts'
            inputs:
              pathToPublish: '$(System.DefaultWorkingDirectory)/dist'
              artifactName: 'frontend'

  - stage: Deploy
    displayName: 'Deploy to VPS'
    dependsOn: Build
    condition: succeeded()
    jobs:
      - job: DeployFrontend
        displayName: 'Deploy to IIS'
        steps:
          - download: current
            artifact: frontend

          - task: PowerShell@2
            displayName: 'Find Browser Folder'
            inputs:
              targetType: 'inline'
              script: |
                $browserFolder = Get-ChildItem -Path "$(Pipeline.Workspace)/frontend" -Filter "browser" -Recurse -Directory | Select-Object -First 1
                
                if ($browserFolder) {
                  $deployPath = $browserFolder.FullName
                  Write-Host "Found browser folder: $deployPath"
                  Write-Host "##vso[task.setvariable variable=DeployPath]$deployPath"
                } else {
                  $firstDir = Get-ChildItem -Path "$(Pipeline.Workspace)/frontend" -Directory | Select-Object -First 1
                  if ($firstDir) {
                    $deployPath = $firstDir.FullName
                    Write-Host "Using folder: $deployPath"
                    Write-Host "##vso[task.setvariable variable=DeployPath]$deployPath"
                  } else {
                    Write-Error "Could not find deployment folder"
                    exit 1
                  }
                }

          - task: PowerShell@2
            displayName: 'Deploy via Remote PowerShell'
            inputs:
              targetType: 'inline'
              script: |
                Write-Host "========================================" -ForegroundColor Cyan
                Write-Host "Deploying Angular Frontend via Remote PS" -ForegroundColor Cyan
                Write-Host "========================================" -ForegroundColor Cyan
                
                $sourcePath = "$(DeployPath)"
                $deploymentPath = "C:\TRADERVPS\Sites\khulafx.com\secure-admin"
                
                Write-Host "Source (local): $sourcePath" -ForegroundColor White
                Write-Host "Target (remote): $deploymentPath" -ForegroundColor White
                Write-Host ""
                
                # Create secure credential object
                $password = ConvertTo-SecureString "$(DEPLOY_PASSWORD)" -AsPlainText -Force
                $credential = New-Object System.Management.Automation.PSCredential("$(deployUser)", $password)
                
                try {
                  # Execute remote commands on VPS
                  $remoteScript = {
                    param($deployPath)
                    
                    Write-Host "Stopping IIS app pool..." -ForegroundColor Yellow
                    Import-Module WebAdministration
                    Stop-WebAppPool -Name "KhulaFxAppPool" -ErrorAction SilentlyContinue
                    Start-Sleep -Seconds 2
                    
                    Write-Host "✅ App pool stopped" -ForegroundColor Green
                    Write-Host "Cleaning destination folder..." -ForegroundColor Yellow
                    
                    # Clean the destination folder completely
                    if (Test-Path $deployPath) {
                      Get-ChildItem -Path $deployPath -Recurse -Force | Remove-Item -Force -ErrorAction SilentlyContinue
                    } else {
                      New-Item -ItemType Directory -Path $deployPath -Force | Out-Null
                    }
                    
                    Write-Host "✅ Destination cleaned" -ForegroundColor Green
                    Write-Host "Ready to receive files" -ForegroundColor Green
                  }
                  
                  # Stop app pool and clean on remote VPS
                  Invoke-Command -ComputerName $(vpsServer) -ScriptBlock $remoteScript -ArgumentList $deploymentPath -Credential $credential -ErrorAction Stop
                  
                  Write-Host ""
                  Write-Host "Copying files to remote server..." -ForegroundColor Yellow
                  
                  # Copy files to VPS
                  $copySession = New-PSSession -ComputerName $(vpsServer) -Credential $credential -ErrorAction Stop
                  
                  Copy-Item -Path "$sourcePath\*" -Destination $deploymentPath -Recurse -Force -ToSession $copySession -ErrorAction Stop
                  
                  Remove-PSSession -Session $copySession -ErrorAction SilentlyContinue
                  
                  Write-Host "✅ Files copied to VPS" -ForegroundColor Green
                  Write-Host ""
                  
                  # Verify files on remote and start app pool
                  $verifyScript = {
                    param($deployPath)
                    
                    Write-Host "Verifying files..." -ForegroundColor Yellow
                    $files = Get-ChildItem -Path $deployPath -Recurse -File
                    Write-Host "✅ Total files: $($files.Count)" -ForegroundColor Green
                    
                    $indexExists = Test-Path "$deployPath\index.html"
                    $webConfigExists = Test-Path "$deployPath\web.config"
                    $robotsExists = Test-Path "$deployPath\robots.txt"
                    
                    Write-Host ""
                    Write-Host "Key files check:" -ForegroundColor Yellow
                    Write-Host "  ✅ index.html: $(if($indexExists){'Found'}else{'MISSING'})" -ForegroundColor $(if($indexExists){'Green'}else{'Red'})
                    Write-Host "  ✅ web.config: $(if($webConfigExists){'Found'}else{'MISSING'})" -ForegroundColor $(if($webConfigExists){'Green'}else{'Red'})
                    Write-Host "  ✅ robots.txt: $(if($robotsExists){'Found'}else{'MISSING'})" -ForegroundColor $(if($robotsExists){'Green'}else{'Red'})
                    
                    if (-not $indexExists -or -not $webConfigExists) {
                      throw "Critical files missing!"
                    }
                    
                    Write-Host ""
                    Write-Host "Starting app pool..." -ForegroundColor Yellow
                    Import-Module WebAdministration
                    Start-WebAppPool -Name "KhulaFxAppPool" -ErrorAction Stop
                    Start-Sleep -Seconds 3
                    
                    Write-Host "✅ App pool started" -ForegroundColor Green
                  }
                  
                  Invoke-Command -ComputerName $(vpsServer) -ScriptBlock $verifyScript -ArgumentList $deploymentPath -Credential $credential -ErrorAction Stop
                  
                  Write-Host ""
                  Write-Host "========================================" -ForegroundColor Green
                  Write-Host "✅ Deployment Complete!" -ForegroundColor Green
                  Write-Host "========================================" -ForegroundColor Green
                }
                catch {
                  Write-Host ""
                  Write-Host "========================================" -ForegroundColor Red
                  Write-Host "❌ Deployment Failed!" -ForegroundColor Red
                  Write-Host "========================================" -ForegroundColor Red
                  Write-Error $_.Exception.Message
                  exit 1
                }

          - task: PowerShell@2
            displayName: 'Verify Website'
            inputs:
              targetType: 'inline'
              script: |
                Write-Host "Waiting for IIS to initialize..." -ForegroundColor Yellow
                Start-Sleep -Seconds 8
                
                Write-Host "Testing website accessibility..." -ForegroundColor Yellow
                Write-Host ""
                
                $urls = @(
                  "https://khulafx.com/secure-admin",
                  "http://khulafx.com/secure-admin",
                  "http://108.181.161.170/secure-admin"
                )
                
                $success = $false
                foreach ($url in $urls) {
                  try {
                    Write-Host "Testing: $url" -ForegroundColor Gray
                    $response = Invoke-WebRequest -Uri $url -UseBasicParsing -TimeoutSec 20 -ErrorAction Stop
                    Write-Host "  ✅ Status: $($response.StatusCode)" -ForegroundColor Green
                    
                    # Check if it's actually Angular
                    if ($response.Content -like "*<app-root>*" -or $response.Content -like "*angular*") {
                      Write-Host "  ✅ Angular app detected" -ForegroundColor Green
                    }
                    
                    $success = $true
                    break
                  }
                  catch {
                    Write-Host "  ⚠️ $($_.Exception.Message)" -ForegroundColor Yellow
                  }
                }
                
                Write-Host ""
                if ($success) {
                  Write-Host "========================================" -ForegroundColor Green
                  Write-Host "✅ Website is Live and Accessible!" -ForegroundColor Green
                  Write-Host "========================================" -ForegroundColor Green
                  Write-Host "URL: https://khulafx.com/secure-admin" -ForegroundColor Cyan
                } else {
                  Write-Warning "Website not accessible yet"
                  Write-Host "If website still doesn't work, check:" -ForegroundColor Yellow
                  Write-Host "  1. IIS Application Pool is running" -ForegroundColor White
                  Write-Host "  2. Firewall allows port 80/443" -ForegroundColor White
                  Write-Host "  3. DNS points to VPS" -ForegroundColor White
                }

  - stage: NotifyComplete
    displayName: 'Notify Completion'
    dependsOn: Deploy
    condition: always()
    jobs:
      - job: SendResult
        displayName: 'Send Telegram Result'
        steps:
          - task: PowerShell@2
            displayName: 'Send Notification'
            inputs:
              targetType: 'inline'
              script: |
                $success = "$(Agent.JobStatus)" -eq "Succeeded"
                $icon = if ($success) { "✅" } else { "❌" }
                $status = if ($success) { "SUCCESS" } else { "FAILED" }
                
                $message = "$icon *KhulaFxAdmin Frontend Deploy $status!*`n`n"
                $message += "📦 *Project:* KhulaFxAdminNew (Angular)`n"
                $message += "🔢 *Build:* $(Build.BuildNumber)`n"
                $message += "👤 *By:* $(Build.RequestedFor)`n"
                $message += "🌿 *Branch:* $(Build.SourceBranchName)`n"
                $message += "💬 *Commit:* $(Build.SourceVersionMessage)`n"
                
                if ($success) {
                  $message += "🌐 *URL:* https://khulafx.com/secure-admin`n"
                }
                
                $duration = [math]::Round((New-TimeSpan -Start '$(System.PipelineStartTime)' -End (Get-Date)).TotalMinutes, 2)
                $message += "⏱️ *Duration:* $duration minutes`n"
                $message += "🔗 [View Logs](https://dev.azure.com/KhulaFx/KhulaFxTrade/_build/results?buildId=$(Build.BuildId))"
                
                $body = @{ chat_id = "$(TELEGRAM_CHAT_ID)"; text = $message; parse_mode = "Markdown" } | ConvertTo-Json
                
                try {
                  Invoke-RestMethod -Uri "https://api.telegram.org/bot$(TELEGRAM_BOT_TOKEN)/sendMessage" -Method Post -ContentType "application/json" -Body $body
                } catch { Write-Warning "Telegram failed: $_" }
