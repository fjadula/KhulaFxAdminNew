import { NgModule } from '@angular/core';

// AppModule kept as an empty module for compatibility. The application
// uses `bootstrapApplication` with standalone components (see src/main.ts).
@NgModule({})
export class AppModule { }